type Query {
  books: [Book]
  bookById(id: ID): Book
  authors: [Author]
}

type Mutation {
  createAuthor(firstName: String, lastName: String): Author
}

type Book {
  id: ID
  title: String
  pageCount: Int
  author: Author
}

type Author {
  id: ID
  firstName: String
  lastName: String
}
